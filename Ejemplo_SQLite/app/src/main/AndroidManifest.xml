<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="appjpm4everyone.ejemplo_sqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistroUsuarioActivity" />
        <activity android:name=".Consulta_Usuario" />
        <activity android:name=".ConsultaComboActivity" />
        <activity android:name=".ConsultaListaListView" />
        <activity android:name=".Detalle_Usuario" />
        <activity android:name=".RegistroMascotaActivity" />
        <activity android:name=".ListaMascotasActivity" />
        <activity android:name=".DetalleMascotaActivity" />
        <activity android:name=".RecyclerListaPersonas"></activity>
    </application>

</manifest>